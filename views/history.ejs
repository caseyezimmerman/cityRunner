<!DOCTYPE html>
<html lang="en">
    <head>
        <% include head %>
        <link rel="stylesheet" type="text/css" href="stylesheets/history.css">
        <title>History</title>
    </head>
    <body>
        <% include navbar%>
        <div class="container">
            <div class="row">
                <h1>History of Routes</h1>
                <!-- loop through runData and render as a table-->
                <% for (var i = 0; i < 1; i++){ %>
                    <form action="/historyProcess" method="post">
                        <div class="col-xs-9">
                            <h3>Previous Run</h3>
                        </div>
                        <div class="col-xs-3">
                            <button type="submit" class="btn btn-primary">Run Again</button>
                        </div>
                    </form>
                <% } %>
            </div>
        </div>
    </body>
</html>